import React from 'react'

const DeleteProfiles = (p) => {
  console.log(`Logging from Delete: ${p}`)

  return (
    <div>
      <button>Delete</button>
    </div>
  )
}

export default DeleteProfiles
